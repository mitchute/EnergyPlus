name: C/C++ CI

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: cmake
      run: mkdir build && cd build && cmake ..
    - name: build
      run: cd build && make energyplus
    - name: test
      run: cd build/Products && ./energyplus -D ../../testfiles/1ZoneUncontrolled.idf
